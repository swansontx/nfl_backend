# NFL Backend (scaffold)

Repository scaffold for the NFL props backend. Focused on backend-only components: ingestion, canonicalization, feature extraction, modeling, calibration/backtest, roster/injury service, and an API service (FastAPI).

This branch: backend-structure
